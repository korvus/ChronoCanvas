module.exports = function set(params){
    var canvasTarget = params.canvasTarget;
    var colorBg = params.bgColor;
    var colorFg = params.fgColor;
    console.log(canvasTarget);

    node = document.querySelector("canvas");
    console.log(node);
};

